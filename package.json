{
  "name": "breakfast-automation",
  "version": "1.0.0",
  "author": "",
  "description": "Śniadanie z Programowaniem automation",
  "main": "index.js",
  "scripts": {
    "build": "(cd webhook && npm run build)",
    "vercel-cloud-build": "npm run build && mkdir -p .output/webhook/dist && cp -r webhook/dist .output/webhook/dist"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "@vercel/node": "^1.12.1"
  },
  "workspaces": [
    "webhook"
  ]
}
